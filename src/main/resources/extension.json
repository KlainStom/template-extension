{
  "entrypoint": "${mainPackage}.${name.toLowerCase()}.ExtensionMain",
  "name": "${name}",
  "version": "${version}",
  "meta": {
    "github": {
      "release": {
        "account": "EXTENSION_REPO_OWNER",
        "repo": "EXTENSION_REPO_NAME",
        "release": "v${version}"
      }
    }
  }
}